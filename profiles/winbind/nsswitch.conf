passwd:     files winbind systemd    {exclude if "with-custom-passwd"}
group:      files winbind systemd    {exclude if "with-custom-group"} {exclude if "with-group-merging"}
group:      files [SUCCESS=merge] winbind [SUCCESS=merge] systemd    {exclude if "with-custom-group"} {include if "with-group-merging"}
