Enable SSSD and fprintd for system authentication

This profile will enable SSSD as a source of identity and authentication
providers. It also enables pam_fprintd module to allow fingerprint
authentication.